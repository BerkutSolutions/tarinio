# Deploy (RU)

Р‘Р°Р·РѕРІР°СЏ РІРµСЂСЃРёСЏ РґРѕРєСѓРјРµРЅС‚Р°С†РёРё: `1.0.15`

## Р‘С‹СЃС‚СЂС‹Р№ AIO-СЃС‚Р°СЂС‚ (РѕРґРЅР° РєРѕРјР°РЅРґР°)

РџРѕР»РЅС‹Р№ СЃС‚РµРє TARINIO (auto-start РїСЂРѕС„РёР»СЊ) РјРѕР¶РЅРѕ РїРѕРґРЅСЏС‚СЊ РѕРґРЅРѕР№ РєРѕРјР°РЅРґРѕР№:

```bash
curl -fsSL https://raw.githubusercontent.com/BerkutSolutions/tarinio/main/scripts/install-aio.sh | sh
```

РџРѕ СѓРјРѕР»С‡Р°РЅРёСЋ СЃРєСЂРёРїС‚:
- РєР»РѕРЅРёСЂСѓРµС‚/РѕР±РЅРѕРІР»СЏРµС‚ СЂРµРїРѕР·РёС‚РѕСЂРёР№ РІ `/opt/tarinio`
- РІС‹Р±РёСЂР°РµС‚ РІРµС‚РєСѓ `main`
- РїРѕРґРЅРёРјР°РµС‚ `deploy/compose/default`

РџРѕСЃР»Рµ Р·Р°РїСѓСЃРєР°:
- `http://<server-ip>:8080/login` (Admin UI)
- `http://<server-ip>/` (WAF HTTP ingress)
- `https://<server-ip>/` (WAF ingress)

## Р СѓС‡РЅРѕР№ Р·Р°РїСѓСЃРє С‡РµСЂРµР· Docker Compose

1. РџСЂРѕРІРµСЂСЊС‚Рµ `.env.example` Рё РїРѕРґРіРѕС‚РѕРІСЊС‚Рµ `.env`.
2. Р—Р°РїСѓСЃС‚РёС‚Рµ РїСЂРѕС„РёР»СЊ:

```bash
docker compose -f deploy/compose/default/docker-compose.yml up -d --build
```

## РџСЂРёРјРµС‡Р°РЅРёСЏ

- Р”Р»СЏ Р±С‹СЃС‚СЂРѕРіРѕ Р»РѕРєР°Р»СЊРЅРѕРіРѕ Р·Р°РїСѓСЃРєР° РёСЃРїРѕР»СЊР·СѓР№С‚Рµ РїСЂРѕС„РёР»СЊ `auto-start`.
- Р”Р»СЏ Р±РѕРµРІРѕРіРѕ Р»РѕРєР°Р»СЊРЅРѕРіРѕ СЃС†РµРЅР°СЂРёСЏ РёСЃРїРѕР»СЊР·СѓР№С‚Рµ `default` (`ui` РЅР° `8080`, `runtime` РЅР° `80/443`).
- Р”Р»СЏ production Р·Р°РґР°РІР°Р№С‚Рµ РЅРµРґРµС„РѕР»С‚РЅС‹Рµ СЃРµРєСЂРµС‚С‹ Рё РІРєР»СЋС‡Р°Р№С‚Рµ HTTPS.



